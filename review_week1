上游服务和下游服务

参考：https://reflectoring.io/upstream-downstream/

upstream和downstream有两个原则，
依赖原则：downstream依赖upstream。
价值原则：通往downstream的每一步都在增加价值，即down'stream的产品拥有更多价值。

但对于微服务中说的上游和下游，需要注意流stream的方法并不是以数据进入系统为上游起点，而是以系统中最底层服务为起点，往用户的方向流动（考虑价值原则，用户获得的最终结果最有价值）。

